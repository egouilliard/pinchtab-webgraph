"""PinchTab Web-Navigation Graph & How-To Path-Finder.

A deterministic (no-LLM) toolkit that operates any website through the PinchTab
browser-automation CLI to build a nav + content graph, then answers how-to
("how do I do X") and where-is ("--find") queries offline.

CLI entry point: `pinchtab_webgraph.cli:main` (installed as `pinchtab-webgraph` / `pwg`).
"""
__version__ = "0.1.0"
